{
  "name": "obsidian-mirror-archive",
  "version": "1.0.0",
  "description": "Move selected files and folders to a configurable mirror archive root while preserving their original vault path.",
  "main": "dist/main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs --production",
    "check": "tsc --noEmit && npm run build && node --check dist/main.js"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "archive"
  ],
  "author": "MapleCheng",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.14",
    "esbuild": "^0.25.1",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.2"
  }
}
